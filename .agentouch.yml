version: 1

app:
  image: ai-agent-php:8.3
  processes:
    web:
      command: >
        sh -lc "
          [ -f .env ] || cp .env.example .env;
          composer install;
          grep -q '^APP_KEY=base64:' .env || php artisan key:generate --force --no-interaction;
          php artisan migrate --force --no-interaction;
          php artisan serve --host=0.0.0.0 --port=8000
        "
